// colors for mesh
const colors: {
    background: string,
    light: string,
    pillar: string,
    floor: string
} = {
    background: '#3e1322',   
    light: '#ffe9ac',
    pillar: '#071d28',
    floor: '#111',
};

export default colors;